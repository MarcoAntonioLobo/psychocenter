spring:
  datasource:
    url: jdbc:postgresql://db:5432/psychocenter
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

# Swagger habilitado apenas em dev
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    enabled: true
